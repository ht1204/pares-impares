<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pares e Impares</title>
  </head>
  <body>
    <% for(let i = 1; i <= 50; i++) { %>
        <p><%=i%> <%=num%> <%=i % 2 === 0 ? 'Par!' : 'Impar!'; %></p>
    <% } %>
  </body>
</html>